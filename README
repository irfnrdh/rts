
$ rts_

    rts adalah sebuah alat untuk meretas ingatan agar
    tidak perlu lagi mengigat perintah cli yang panjang 
    dengan mudah.

    filosofinya jangan direbitin.

Penggunaan:

    $ pip install --editable .
    $ rts --help

<!-- 
## Shell
```
    $ eval (env _RTS_COMPLETE=fish_source rts)
    $ eval "$(_RTS_COMPLETE=zsh_source rts)"
    $ eval "$(_RTS_COMPLETE=bash_source rts)"
```
## Testing
```
    $ virtualenv venv
    $ . venv/bin/activate
    $ pip install --editable .
```
 -->